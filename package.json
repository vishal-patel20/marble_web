{
  "name": "marble-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs'); const cp=require('child_process'); if(fs.existsSync('./client')){ cp.execSync('cd client && npm install --legacy-peer-deps && npm run build', {stdio:'inherit'}); } else { cp.execSync('npm run build', {stdio:'inherit'}); }\""
  }
}
